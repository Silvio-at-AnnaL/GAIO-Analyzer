kind = "web"
previewPath = "/"
title = "GAIO Analyzer"
version = "1.0.0"
id = "artifacts/gaio-analyzer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20944

[services.development]
run = "pnpm --filter @workspace/gaio-analyzer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/gaio-analyzer", "run", "build" ]
publicDir = "artifacts/gaio-analyzer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20944"
BASE_PATH = "/"
