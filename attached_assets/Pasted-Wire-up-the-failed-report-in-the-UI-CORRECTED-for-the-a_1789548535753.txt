Wire up the failed-report in the UI — CORRECTED for the actual component structure (Task 2, final part
2c). My earlier spec wrongly targeted ReportView; verified reality: a FAILED analysis stays in
ProgressView (analysisStatus never becomes "completed", so ReportView never mounts). ProgressView already
has full `report` data (via useGetAnalysisReport) including crawlReliability and errors, and already shows
the failed view. So BOTH the auto-export and the download button must live in ProgressView. Do NOT change
routing. Do NOT touch ReportView's existing completed auto-export. ALL new UI text via i18n. Do NOT touch
the backend or report-export.ts. Do NOT publish. Report back before pushing.

═══════════════════════════════════════════════════════════════════
CONTEXT (verified)
═══════════════════════════════════════════════════════════════════
File: artifacts/gaio-analyzer/src/views/ErgebnisseView.tsx
- ProgressView (function at ~line 70): shown while analysisStatus === "running"; a failed analysis stays
  here because analysisStatus is only ever set to "completed". It reads `report` via useGetAnalysisReport
  (~line 76) — so report.status === "failed", report.crawlReliability, report.errors, report.url are all
  available here. The failed view (title/message) is rendered here (isFailed branch ~lines 99-145).
- ReportView (function at ~line 672): mounts only when completed; its auto-export useEffect (~line 715)
  handles COMPLETED reports. LEAVE IT UNTOUCHED.
- generateHtmlReport already returns the branded FAILED report when report.status === "failed".
- The completed auto-export POSTs to /api/admin/analysis-log/auto-export with
  { htmlContent, logId, domain, companyName, gaioScore, scoresJson, pagesCrawled }.
- A client-side download pattern exists elsewhere in the file (Blob → createObjectURL → a.download).

═══════════════════════════════════════════════════════════════════
1) ProgressView: auto-export the failed report ONCE, when status becomes "failed"
═══════════════════════════════════════════════════════════════════
Add a useEffect in ProgressView that fires when report?.status === "failed" (guard with a ref so it runs
only once, mirroring ReportView's autoSavedRef pattern). It should:
  - call generateHtmlReport(report, { inputParams: <same shape ReportView builds>, contactData: <same> })
    — the function already produces the failed document for a failed report.
  - POST to /api/admin/analysis-log/auto-export with:
      htmlContent, logId: (report.logId ?? null), domain: report.url ?? "",
      companyName: domainForm.companyName?.trim() || "", gaioScore: null, scoresJson: null,
      pagesCrawled: report.crawledPages?.length ?? 0
  - This makes the failed analysis downloadable from the Analyseprotokoll (html_export_id gets set),
    exactly like completed ones.
  Look at how ReportView builds inputParams/contactData for its auto-export and reuse the SAME construction
  (extract a small shared helper if that's cleanest, or replicate the exact shape — do not invent new
  fields). If building inputParams/contactData in ProgressView needs data ProgressView doesn't have,
  report that back instead of guessing.

2) ProgressView failed view: a download button
   In the isFailed branch (~lines 99-145), below the failure message, add a button that generates the
   failed report and downloads it client-side (reuse the Blob/createObjectURL/a.download pattern; filename
   like `GAIO-Analyse-<domain>-<date>.html`). Show a brief loading state while generating.

═══════════════════════════════════════════════════════════════════
3) i18n — new keys, group matching the neighbouring failed-view labels
═══════════════════════════════════════════════════════════════════
   (progress.* if the failed title/message nearby use progress.*, else results.*)
   *.failed_download_button   "Analyse-Ergebnis herunterladen"
   *.failed_download_hint     "Sie können dieses Ergebnis als Nachweis an das untersuchte Unternehmen weitergeben."
   *.failed_download_loading  "Wird erstellt…"

═══════════════════════════════════════════════════════════════════
Verification (report back, do NOT push)
═══════════════════════════════════════════════════════════════════
- Frontend typecheck passes.
- Guaranteed-failed analysis (dead domain): confirm (a) the failed report auto-uploads (analysis_exports
  row + html_export_id set → downloadable from Analyseprotokoll with the export icon), and (b) the failed
  view shows the download button, which downloads the branded failed report (error block + crawl-reliability
  evidence + Kontakt).
- Confirm the COMPLETED path is 100% unchanged — ReportView's existing auto-export still fires once for a
  normal analysis, and you did NOT modify ReportView.
- Confirm the failed export fires exactly once (ref guard).
- Under EN, new labels fall back to German.
- Report exact lines changed; confirm ReportView, backend, and report-export.ts untouched.