SAFETY FIX — neutralize the unused Drizzle template package (lib/db). Narrow scope.

Context:
lib/db is an unused Replit template. GAIO's real DB layer is raw SQL in artifacts/api-server/src/lib/db*.
lib/db/src/schema/index.ts exports nothing, so drizzle-kit sees an EMPTY schema. Running
`drizzle-kit push` against DATABASE_URL (the production Neon DB) proposes dropping ALL GAIO tables;
`push-force` drops them without confirmation. scripts/post-merge.sh currently runs `pnpm --filter db push`
after every merge. Goal: make this impossible. Do NOT delete the package.

Tasks:
1. scripts/post-merge.sh: remove the line `pnpm --filter db push`. Keep `#!/bin/bash`, `set -e` and
   `pnpm install --frozen-lockfile` unchanged. Add one comment line explaining that db push was removed
   on purpose because lib/db is an unused template and push would drop production tables.
2. lib/db/package.json: remove the "push" and "push-force" scripts. Leave "scripts" as an empty object.
   Do not change dependencies or anything else in the file.
3. lib/db/drizzle.config.ts: replace the entire content with a short comment plus an unconditional
   `throw new Error("drizzle-kit is disabled in this project: lib/db is an unused template. Running push would drop all GAIO production tables.");`
   followed by `export {};`. No defineConfig, no imports.
4. replit.md, line with "**Database**: PostgreSQL + Drizzle ORM ...": replace with
   "**Database**: PostgreSQL (Neon), accessed via raw SQL in artifacts/api-server/src/lib/db*. lib/db is an unused Drizzle template."
   Then add a short section "## Database safety" stating: never run drizzle-kit push / push-force / db push
   against this project's database; lib/db must not be used as schema source.

Do NOT:
- delete lib/db or any file
- touch pnpm-lock.yaml, any other package.json, .replit, or api-server code
- run pnpm install, any drizzle-kit command, or connect to any database

Verification (report the output):
- full `git diff`
- `bash -n scripts/post-merge.sh` (syntax check)
- `grep -rn "drizzle-kit push\|filter db\|push-force" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=attached_assets .`
  → expected: only the warning text in replit.md and the comment/error text in the changed files
- `pnpm --filter @workspace/api-server run typecheck` → must still pass

Do not push or publish. Report back first.