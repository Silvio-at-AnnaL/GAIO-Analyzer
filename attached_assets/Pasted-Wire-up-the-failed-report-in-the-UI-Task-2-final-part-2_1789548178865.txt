Wire up the failed-report in the UI (Task 2, final part 2c). The failed-report variant already exists in
generateHtmlReport, but the frontend never generates/uploads it for failed analyses, and the failed view
shows no way to get the report. Two changes in ErgebnisseView.tsx: (1) make the auto-export effect ALSO
run for failed analyses, (2) give the user a download button in the failed view. ALL new UI text via the
i18n label system (standing rule). Do NOT touch the backend or report-export.ts. Do NOT publish. Report
back before pushing.

═══════════════════════════════════════════════════════════════════
CONTEXT
═══════════════════════════════════════════════════════════════════
File: artifacts/gaio-analyzer/src/views/ErgebnisseView.tsx
- The auto-export useEffect (~line 715) currently starts with:
    if (report?.status !== "completed" || autoSavedRef.current) return;
  It then calls generateHtmlReport(report, {...}) and POSTs to /api/admin/analysis-log/auto-export with
  { htmlContent, logId, domain, companyName, gaioScore, scoresJson, pagesCrawled } (~lines 746-780).
- generateHtmlReport already returns the branded FAILED report when report.status === "failed" — so the
  SAME call works for both; only the guard and the score-ish payload fields differ.
- The failed view is at ~lines 99-145 (isFailed branch), currently showing only the failure title/message,
  no download.
- A working client-side download pattern exists at ~line 1772 (URL.createObjectURL(blob) + a.download).

═══════════════════════════════════════════════════════════════════
1) Auto-export effect: also run for FAILED
═══════════════════════════════════════════════════════════════════
Change the guard so the effect runs for BOTH completed and failed, and its dependency array includes the
status. Keep the autoSavedRef guard (upload once).
    if ((report?.status !== "completed" && report?.status !== "failed") || autoSavedRef.current) return;
Inside, generateHtmlReport(report, {...}) already produces the right document for either status — no branch
needed there. For the POST body, the score fields are meaningless when failed: send gaioScore: null and
scoresJson: null (or an empty object) when report.status === "failed"; keep the existing values when
completed. pagesCrawled can be report.crawledPages?.length ?? 0 in both cases. Everything else (logId,
domain, companyName) stays. This makes the backend store the FAILED report's HTML the same way it stores
a completed one, so it becomes downloadable from the protocol too.

═══════════════════════════════════════════════════════════════════
2) Failed view: a download button
═══════════════════════════════════════════════════════════════════
In the isFailed branch (~lines 99-145), below the failure message, add a button that generates the failed
report HTML and downloads it client-side (so the salesperson gets the file immediately, without going to
the protocol). Reuse the existing download pattern (generateHtmlReport → Blob → createObjectURL →
a.download with a sensible filename like `GAIO-Analyse-<domain>-<date>.html`). Wire it to an onClick
handler; show a brief loading state while generating.

New i18n keys (group "results" or "progress" — match where the surrounding failed-view text lives):
  results.failed_download_button      "Analyse-Ergebnis herunterladen"
  results.failed_download_hint        "Sie können dieses Ergebnis als Nachweis an das untersuchte Unternehmen weitergeben."
  results.failed_download_loading     "Wird erstellt…"
(Use whatever group the neighbouring failed-view labels already use; if they use "progress.*", put these
under progress.* instead — keep it consistent with the adjacent text.)

═══════════════════════════════════════════════════════════════════
Verification (report back, do NOT push)
═══════════════════════════════════════════════════════════════════
- Frontend typecheck passes.
- Run a guaranteed-failed analysis (dead domain). Confirm:
    * The auto-export effect now fires: an analysis_exports row + html_export_id get set for the failed
      analysis (so it's downloadable from the Analyseprotokoll, with the HTML-Export icon like completed
      ones).
    * The failed view shows the new "Analyse-Ergebnis herunterladen" button; clicking it downloads the
      branded failed report HTML (error block + crawl-reliability evidence + Kontakt).
- Confirm a completed analysis still auto-exports exactly as before (no regression — the guard change must
  not break the completed path; verify a normal analysis still uploads once).
- Under EN, the new labels fall back to German.
- Report exact lines changed; confirm backend and report-export.ts untouched.