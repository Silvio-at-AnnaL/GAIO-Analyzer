Make a FAILED crawl produce a downloadable result (Task 2, part 1 of 3: backend persistence). When a
crawl fails with zero usable pages, the analysis currently does `save(); return;` and never records the
failure in analysis_log or generates a downloadable HTML — so it's stuck as "running" in the protocol and
there's nothing to download. Fix the persistence so a failed crawl becomes a proper, downloadable "failed"
record with an error HTML export. This part does NOT build the fancy sales report yet (part 2) — it wires
the plumbing and generates a minimal-but-real error HTML so the download works end to end. Do NOT touch
the frontend. Do NOT publish. Report back before pushing.

═══════════════════════════════════════════════════════════════════
CONTEXT
═══════════════════════════════════════════════════════════════════
- analysis-engine.ts: the zero-pages failure branch is at ~lines 316-326 (`if (pages.length === 0) {
  state.status="failed"; state.errors.push(...); save(); return; }`). There is a second similar failed
  path for the explicit-URL empty case; check for both.
- The success path (~505-525) calls createAnalysisLog at start (logId, line 179) and
  updateAnalysisLogComplete at the end. The catch block (~539) calls updateAnalysisLogFailed. The early
  `return` in the crawl-failure branch SKIPS all of that — that's the bug.
- state fields available at failure: state.errors (the differentiated German message), state.crawlReliability
  (attempted/succeeded/failed + failures[] with the dead URLs), state.crawledPages, state.url,
  the company name.
- generateHtmlReport lives in the FRONTEND (report-export.ts) and cannot be called from the backend.
  The success-path HTML export is done in routes/admin.ts via saveAnalysisExport with html generated
  elsewhere. Check how completed analyses get their html_export: find where saveAnalysisExport is called
  for a normal completed analysis and mirror that mechanism for the failed case.

═══════════════════════════════════════════════════════════════════
1) analysis-engine.ts — record the failure in analysis_log before returning
═══════════════════════════════════════════════════════════════════
In BOTH crawl-failure branches (zero-pages timeout/unreachable case, and the explicit-URL empty case),
BEFORE `save(); return;`, call updateAnalysisLogFailed so the protocol shows "failed" (not stuck at
"running"). Pass the user-facing German error message (state.errors[state.errors.length-1] or the message
you just pushed). Wrap in try/catch like the existing catch block does (a DB hiccup must not crash the
engine). Keep the early return.

    if (logId !== null) {
      try { await updateAnalysisLogFailed(logId, <the german error message>); }
      catch (dbErr) { logger.error({ dbErr }, "Failed to update analysis_log for failed crawl"); }
    }
    save();
    return;

Also make sure state carries enough for the report: state.crawlReliability is already set (good). Ensure
state.errors holds the differentiated message (it does). Do not change the messages.

═══════════════════════════════════════════════════════════════════
2) Generate + save an ERROR HTML export for the failed analysis
═══════════════════════════════════════════════════════════════════
Find how a COMPLETED analysis's HTML gets into analysis_exports (saveAnalysisExport). Two possibilities —
investigate and follow the existing pattern:
  (a) If the backend already builds/receives the HTML for completed analyses at some point it controls,
      build a MINIMAL error HTML string there for the failed case and call saveAnalysisExport(logId,
      errorHtml). 
  (b) If completed-analysis HTML is generated on the FRONTEND and uploaded via an endpoint, then the
      backend cannot render the full report — in that case, for THIS part, generate a small SELF-CONTAINED
      error HTML string directly in the backend (plain template literal, inline styles, German) and save
      it via saveAnalysisExport. It must be valid standalone HTML.

The minimal error HTML for part 1 must contain (real data from state):
  - A clear German headline: "Analyse nicht möglich – [company name or domain]"
  - The failure reason (state.errors — the differentiated message)
  - A short reliability summary: "Seiten versucht: N · Erfolgreich: M · Fehlgeschlagen: K"
  - The first up-to-25 failed URLs with their reason/status (from state.crawlReliability.failures)
  - The analysis date and the analysed URL
  - Keep styling minimal but clean (this gets replaced by the full sales report in part 2 — do NOT
    over-invest in layout here; correctness and real data matter).
Escape all dynamic values (URLs, company name) to prevent broken HTML.

Report back HOW completed analyses currently get their HTML into analysis_exports (which mechanism a/b),
so part 2 can plug into the same place.

═══════════════════════════════════════════════════════════════════
Verification (report back, do NOT push)
═══════════════════════════════════════════════════════════════════
- API typecheck passes.
- Run an analysis of https://inden-gmbh.de but FORCE the zero-pages failure to test the failed path — OR,
  if inden-gmbh.de now succeeds (it gathered 12 pages last time), instead analyse a guaranteed-dead URL
  (e.g. https://this-domain-does-not-exist-gaio-test-99.de) so pages.length === 0 and the failed branch
  runs. Confirm:
    * analysis_log row for it shows status "failed" (not "running") with the German error_message.
    * an analysis_exports row exists for it (the error HTML was saved).
    * the saved HTML contains the headline, the reason, the reliability summary, and the failed-URL list.
- Confirm a NORMAL completed analysis still logs + exports exactly as before (no regression).
- Report which mechanism (a or b) completed-analysis HTML uses, and the exact lines you changed.
- Backend only.