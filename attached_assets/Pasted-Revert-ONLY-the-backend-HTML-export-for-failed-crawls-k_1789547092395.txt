Revert ONLY the backend HTML export for failed crawls (keep the failed-status logging). We decided the
FRONTEND will be the single source of report HTML for both success and failure, so the backend must NOT
save its own error HTML anymore — otherwise it sets html_export_id first and the auto-export endpoint
then skips the frontend's nicer report. Do NOT touch the frontend. Do NOT publish. Report back before
pushing.

Context: In the previous change (analysis-engine.ts), the zero-page failure branch now calls BOTH
updateAnalysisLogFailed AND saveAnalysisExport(logId, buildFailedCrawlHtml(...)). Keep the first, remove
the second.

Changes in artifacts/api-server/src/lib/analysis-engine.ts:
1. In the zero-page failure branch (~lines 388-401), KEEP the updateAnalysisLogFailed call and its
   try/catch (the protocol must still show "failed" with the German error_message). REMOVE the
   saveAnalysisExport(...) call and its try/catch — the backend no longer writes error HTML.
2. Remove the now-unused buildFailedCrawlHtml function (and the escapeHtml helper IF it is used nowhere
   else in this file — check first; if escapeHtml is used elsewhere, keep it). Remove the saveAnalysisExport
   import IF it is now unused in this file (check — it may be imported but only used here). Do not remove
   updateAnalysisLogFailed or updateAnalysisLogComplete imports.
3. Leave everything else — the differentiated German error messages, the crawl reliability data on state,
   the early return, the success path — exactly as is.

Rationale to encode as a comment near the failure branch:
  "// HTML report (success AND failure) is generated by the frontend and uploaded via
  //  /api/admin/analysis-log/auto-export. The backend only records the failed status here."

Verification (report back, do NOT push):
- API typecheck passes; no unused-import or unused-function warnings remain.
- Analyse a guaranteed-dead URL (e.g. https://this-domain-does-not-exist-gaio-test-99.de): the
  analysis_log row is still "failed" with the correct German error_message, but NO analysis_exports row
  is created by the backend anymore (html_export_id stays null until the frontend uploads one).
- A normal completed analysis is unaffected.
- Report exact lines removed; confirm buildFailedCrawlHtml/escapeHtml/import cleanup is consistent (no
  dead code, no broken references).
- Backend only.