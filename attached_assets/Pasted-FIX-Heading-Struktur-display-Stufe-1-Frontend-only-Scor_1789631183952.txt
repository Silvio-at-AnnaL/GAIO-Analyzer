FIX — Heading-Struktur display (Stufe 1). Frontend only. Score logic unchanged.

Context:
artifacts/gaio-analyzer/src/lib/report-export.ts (~line 491-500) reads
`headingStructure.h1Tags.count` and `headingStructure.issues`. These fields never existed.
The backend (artifacts/api-server/src/lib/analyzers/headings.ts) returns
`{ score, pages: [{ url, h1Count, hasHierarchyIssues, headings }], keywordInHeadings }`.
Result: the HTML report shows "H1-Tags: 0" for every domain. The live view (ErgebnisseView.tsx,
ReportView, also the PDF source) has no heading detail card at all.
Goal: show correct heading data, derived from `pages`, identically in both renderers.
It must also work for already stored analyses.

Tasks:
1. New file artifacts/gaio-analyzer/src/lib/heading-summary.ts exporting a pure function
   `getHeadingSummary(headingStructure: unknown)`. It returns null if `pages` is missing or not an array.
   Otherwise it returns:
     { totalPages, pagesWithSingleH1, pagesWithoutH1, pagesWithMultipleH1, pagesWithHierarchyIssues,
       problemPages: Array<{ url: string; h1Count: number; hasHierarchyIssues: boolean }> }
   Rules:
   - single = h1Count === 1
   - without = h1Count === 0
   - multiple = h1Count > 1
   - problemPages = pages where h1Count !== 1 OR hasHierarchyIssues === true, in original order (full list;
     the renderers cap the display).
   Defensive typing: skip page entries without a string url or a numeric h1Count.
   No React, no i18n in this file.

2. labelDefaults.ts, group "results", add these keys directly after "results.content_relevance_title":
   "results.headings_card_title":        "Heading-Struktur"
   "results.headings_score_label":       "Score"
   "results.headings_pages_total":       "Analysierte Seiten"
   "results.headings_single_h1":         "Seiten mit genau einer H1"
   "results.headings_no_h1":             "Seiten ohne H1"
   "results.headings_multiple_h1":       "Seiten mit mehreren H1"
   "results.headings_hierarchy":         "Seiten mit Hierarchiefehler (H3 vor erster H2)"
   "results.headings_problem_label":     "Seiten mit Auffälligkeiten"
   "results.headings_reason_no_h1":      "keine H1"
   "results.headings_reason_multi_h1":   "{n} H1-Überschriften"
   "results.headings_reason_hierarchy":  "H3 vor erster H2"
   "results.headings_more":              "… und {n} weitere"
   "results.headings_all_ok":            "Keine Auffälligkeiten auf den analysierten Seiten."
   Use the existing placeholder syntax {n} (single braces), as in "domain.pages_show_more".

3. ErgebnisseView.tsx, ReportView: add a new Card between the Schema.org card and the
   contentRelevance card. Copy the markup pattern of the FAQ card (CardHeader/CardTitle classes,
   2-column grid, score in font-mono with scoreBadgeColor).
   - Render the card only if `headingStructure` exists.
   - Always show the score.
   - If getHeadingSummary returns non-null, also show the 5 counts, then either the problem list or the
     all_ok text.
   - Problem list: max 10 entries, each = URL (text, break-all) + reasons joined with ", ";
     if more than 10, add the headings_more line.
   - All texts via t(). No hardcoded strings.

4. report-export.ts, heading block (~line 491): remove the `h1Tags` and `issues` reads.
   Use getHeadingSummary instead.
   - Put all new German texts in a named constant block `HT = { ... }` inside the `if (headings)` block,
     following the existing `ST` / `FT` pattern (this file is not yet i18n-migrated).
     Wording must match the new labelDefaults values.
   - Keep the existing <h3>, the detail-grid and the score item.
   - Add detail-items for the 5 counts.
   - Add the problem list (max 10 + "more" line) or the all_ok text, styled like the removed issues list.
   - Escape URLs with the existing esc() helper.
   - If the summary is null, show only the score (no "0" values).

Do NOT:
- touch anything under artifacts/api-server, lib/api-spec, lib/api-zod, lib/api-client-react
- change any score calculation
- change the methodology/FAQ tables in report-export.ts
- run pnpm install or codegen

Verification (report the output):
- full `git diff --stat` and `git diff`
- `pnpm --filter @workspace/gaio-analyzer run typecheck` → must pass
- `git grep -n "h1Tags" -- artifacts` → expected: no matches
- count of keys in labelDefaults.ts before and after (expected +13)
- Run one analysis for https://www.steinbach-kugellager.de on dev. Report the heading values shown in
  the live view and in the downloaded HTML report. They must be identical.
  Expected: every analysed page has exactly 1 H1; a few pages may show "H3 vor erster H2".

Do not push or publish. Report back first.