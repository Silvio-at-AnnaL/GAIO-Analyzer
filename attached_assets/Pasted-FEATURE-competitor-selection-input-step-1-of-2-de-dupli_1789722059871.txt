FEATURE — competitor selection & input, step 1 of 2: de-duplication, own-domain exclusion, honest limit.
No report rendering changes in this step (that is step 2).

Files in scope:
- artifacts/gaio-analyzer/src/lib/utils.ts
- artifacts/gaio-analyzer/src/views/DomainAnalyseView.tsx
- artifacts/gaio-analyzer/src/lib/labelDefaults.ts (group "domain" only)
- artifacts/api-server/src/lib/analysis-engine.ts
- artifacts/api-server/src/routes/prefill.ts

Confirmed problems (live report, identbase 18.09.): the user had entered evolis.com FOUR times among
8 entries. Result: evolis appeared twice in the comparison table with identical scores, and
maticagroup.com was silently dropped because analyzeCompetitors caps at MAX_COMPETITORS = 5.
The form allows 10 entries while only 5 are ever analysed.

1. lib/utils.ts — add and export `competitorKey(input: string): string`:
   parse the URL (prefix "https://" when missing), lowercase the hostname, strip a leading "www.",
   return "" when it cannot be parsed. Host only — no path, no protocol.

2. DomainAnalyseView.tsx
   a) Maximum 5 competitor fields: change the "add" condition from `.length < 10` to `.length < 5`.
   b) Below the competitor list, render a hint line with t("domain.competitor_limit_note").
   c) Duplicate/own-domain marking, computed on render (not stored):
      for each filled field, compute competitorKey. Mark a field as
      - "own" when the key equals competitorKey(domainForm.url)
      - "duplicate" when an EARLIER filled field has the same key
      Show an amber AlertTriangle next to that field (same styling as the existing unverified marker)
      with title = t("domain.competitor_duplicate") resp. t("domain.competitor_own_domain").
      Keep the existing verified/unverified markers; when a field is "own" or "duplicate", show only
      the new marker.
   d) When starting the analysis (where competitorUrls is built, ~line 210): drop empty entries, drop
      entries whose key is "" , drop own-domain entries, and keep only the FIRST entry per key.
      Then join with "\n" as today. Do not change anything else about the request.
   e) Prefill success handler: apply the same filtering to result.competitors before putting them into
      the form (unique by key, no own domain), keeping order.

3. labelDefaults.ts, group "domain" — add:
   "domain.competitor_limit_note":  "Es werden bis zu 5 Wettbewerber analysiert."
   "domain.competitor_duplicate":   "Dieselbe Domain ist bereits eingetragen – sie wird nur einmal analysiert."
   "domain.competitor_own_domain":  "Das ist Ihre eigene Domain – sie wird nicht als Wettbewerber analysiert."

4. analysis-engine.ts — make the server independent of the form.
   In extractCompetitorUrls (or right after it), using the same host-key rule as the frontend
   (lowercase host without "www."):
   - drop entries whose key is empty
   - drop entries whose key equals the key of the analysed URL
   - keep only the first entry per key, preserving order
   Then cap the list at 5 IN THE ENGINE before calling analyzeCompetitors
   (leave the MAX_COMPETITORS guard in competitors.ts as a safety net).
   Record the outcome on the analysis state as `competitorInput`:
     { provided, analysed, duplicatesRemoved, ownDomainRemoved, droppedByLimit: string[] }
   (droppedByLimit = the unique URLs beyond the cap, in order). Persist it with the rest of the state
   so the report can show it in step 2, and log it once (info) as "competitor input normalized".

5. prefill.ts — apply the same rule to the suggestion list before returning it: unique by host key,
   never the analysed domain itself. Do not change the verification logic or the AI prompt.

Do NOT:
- change analyzeCompetitors' scoring, crawling or timeouts
- change the report renderers (ErgebnisseView, report-export) — that is step 2
- touch lib/api-spec, lib/api-zod, or run pnpm install / codegen

Verification (report the output):
- full `git diff` and both typechecks
  (`pnpm --filter @workspace/api-server run typecheck`, `pnpm --filter @workspace/gaio-analyzer run typecheck`)
- Throwaway script, NOT committed (esbuild CJS), for the engine-side normalization with the real
  identbase input, analysed URL https://identbase.de/ :
    https://www.youcard24.de/ , https://cards-x.de/ , https://www.evolis.com , https://www.zebra.com ,
    https://www.evolis.com , https://www.evolis.com , https://www.evolis.com , https://www.maticagroup.com
  Expected: 5 unique URLs analysed (youcard24, cards-x, evolis, zebra, maticagroup),
  duplicatesRemoved 3, droppedByLimit empty.
  Second case: add https://identbase.de/ and https://www.identbase.de/ to that list →
  ownDomainRemoved 2, result unchanged.
  Third case: 7 unique competitors → analysed 5, droppedByLimit contains the last 2.
- In the browser (dev): enter a domain plus the same competitor twice and the own domain; confirm the
  amber markers and the hint line appear, and that the "add" button disappears at 5 fields.
  Report what you see (screenshot description is enough).

Do not push or publish. Report back first.