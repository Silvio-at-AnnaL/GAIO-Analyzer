```
Please make the following four changes. Each is 
independent — fix all of them.

---

CHANGE 1 — Enforce German language in all 
AI-generated recommendations

The recommendations generated by the AI (Kritisch, 
Hoher Hebel, Nachgeordnet tiers) regularly contain 
English text mixed with German. This must be fixed 
at the source.

Find the prompt that is sent to the AI to generate 
recommendations and add the following instruction 
explicitly and prominently in the prompt:

  "IMPORTANT: You must respond entirely in German. 
  Every word of every recommendation — the title, 
  the problem description, the why_it_matters 
  explanation, and the fix instruction — must be 
  in German. Do not use any English words, phrases, 
  or sentences anywhere in your response. This is 
  a strict requirement."

Also check if there are any other AI prompts in 
the codebase (content analysis, LLM simulation, 
competitor insights, executive summary) that do 
not already enforce German output. Add the same 
German-language instruction to any prompt that 
is missing it.

---

CHANGE 2 — Update PDF footer text

Find the code that renders the footer line at 
the bottom of each PDF page. It currently outputs:
  "GAIO Analyzer · gaio-analyzer.com · 
  Exportiert am [DATUM]"

Change this to:
  "IndustryStock.com/GAIO-Analyzer · 
  Exportiert am [DATUM]"

The date format stays the same as currently used.
Apply this to every PDF page including the 
Details, LLM, Wettbewerb, Empfehlungen, FAQ, 
Analyseparameter, and Kontakt pages.

---

CHANGE 3 — Fix Kontakt page content overflow 
in PDF

The Kontakt page content is being cut off on 
the right side. This is caused by content 
exceeding the iframe width during capture.

Apply these fixes to the Kontakt iframe HTML:

  a) Set the body width explicitly:
       body { width: 1176px; /* 1200px - 24px padding */ 
              overflow: hidden; }

  b) Ensure all child elements respect the 
     container width:
       * { max-width: 100%; box-sizing: border-box; }

  c) For the contact card layout (logo + profile 
     image + text): if it uses flexbox or a 
     side-by-side layout, switch to a vertical 
     stacked layout:
       .contact-card { 
         display: flex; 
         flex-direction: column; 
         gap: 16px; 
       }
     This prevents horizontal overflow when the 
     iframe is captured.

  d) The profile image and logo must have 
     explicit max-width constraints:
       img { max-width: 200px; height: auto; }

  e) Long text (email address, company name) 
     must wrap:
       word-break: break-word; 
       overflow-wrap: break-word;

  f) After making these changes, increase the 
     iframe wait time to 1200ms before capture 
     to ensure the layout is fully settled.

---

CHANGE 4 — Footer missing on content pages 
(Details, LLM, Wettbewerb, Empfehlungen)

These pages often have no footer because the 
page height is calculated from content height 
alone and the footer is either outside the 
captured area or not included in the HTML at all.

Fix this by explicitly including the footer 
inside each captured panel's content area 
during PDF export — not as a separate element 
added by jsPDF afterward.

Approach:

Before capturing each tab panel (details, llm, 
competitors, recommendations), inject a footer 
div at the bottom of the panel:

  const footer = document.createElement('div')
  footer.id = 'pdf-footer-injected'
  footer.style.cssText = `
    width: 100%;
    margin-top: 32px;
    padding-top: 12px;
    border-top: 1px solid #dde0e8;
    font-size: 11px;
    color: #9ca3af;
    text-align: center;
    font-family: -apple-system, sans-serif;
  `
  footer.textContent = 
    'IndustryStock.com/GAIO-Analyzer · ' +
    'Exportiert am ' + currentDateString

  panel.appendChild(footer)

  // Then wait for reflow:
  void panel.offsetHeight
  await new Promise(r => setTimeout(r, 200))

  // Then capture as usual

After capture, remove the injected footer:
  const injected = panel.querySelector(
    '#pdf-footer-injected'
  )
  if (injected) panel.removeChild(injected)

This ensures the footer is always part of 
the captured content and appears at the bottom 
of every page regardless of content length.

Apply the same approach to the FAQ, 
Analyseparameter, and Kontakt pages — but for 
those, the footer is already in the iframe HTML, 
so just update the text there to match the new 
format from Change 2.
```