SMALL FIX — AI recommendations guard: rule-based topic duplicates + case-sensitive schema type names.
One file only.

File: artifacts/api-server/src/lib/analyzers/recommendations.ts (no other file).

1. New guard rule "rule_based_topic", checked FIRST, before the existing rules. It applies always.
   Drop an AI recommendation if its lowercased headline matches
     /(robots\.txt|robots-txt|sitemap|llms\.txt|llms-txt)/
   Reason: robots.txt, sitemap and llms.txt are covered by the rule-based recommendations;
   AI items on these topics are duplicates.
   Logging as for the other rules.

2. schema_total_absence: the type-name exception must be CASE-SENSITIVE and checked against the
   ORIGINAL-case headline (same cut: up to the first ":" or ". ", max 160 chars).
   Keep the subject/absence regex checks on the lowercased headline as today.
   Replace SCHEMA_TYPE_NAMES with this exact list (original casing, word-boundary match):
     Product, WebPage, WebSite, Article, NewsArticle, Organization, LocalBusiness, FAQPage,
     BreadcrumbList, Offer, Review, AggregateRating, HowTo, ImageObject, VideoObject
   (Service, Person and Event are removed on purpose: they collide with common German words.)

3. No other changes. The guard still applies to AI recommendations only; rule-based recommendations
   are never filtered.

Verification (report the output):
- full `git diff` and `pnpm --filter @workspace/api-server run typecheck`
- Throwaway script, NOT committed (esbuild CJS, NODE_ENV=production). Use the same moduleResults as in
  Part 2 and re-run all 8 previous fixture strings: the expected results must be unchanged.
  Then add these fixtures:
  Expected DROPPED:
    [rule_based_topic]     "LLM-Crawler in robots.txt nicht explizit adressiert: GPTBot und ClaudeBot werden nicht genannt."
    [rule_based_topic]     "Sitemap unvollständig: Nur 60 % der analysierten Seiten sind enthalten."
    [schema_total_absence] "Keine strukturierten Daten auf der Website: Es wurde kein Markup gefunden."
    [schema_total_absence] "Strukturierte Daten fehlen komplett für Service-Seiten: Kein Markup auf den Leistungsseiten."
  Expected KEPT:
    "Fehlende Schema-Typen Product und WebPage: Beide Typen wurden auf den analysierten Seiten nicht gefunden."
    "WebSite-Schema fehlt komplett: Der Typ WebSite ist nicht implementiert."
  Print kept/dropped with the rule id for all 14 strings.
- No model call and no analysis run needed.

Do not push or publish. Report back first.